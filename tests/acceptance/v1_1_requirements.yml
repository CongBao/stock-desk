schema_version: 1
requirements:
  - id: V11-R-001
    category: market
    kind: architecture
    behavior_key: index_and_equity_identity_are_distinct
    acceptance: "The instrument identity model distinguishes indices from listed equities across search, routing, persistence, and display; the Shanghai Composite is canonicalized as 000001.SS and is the safe default when no stock has been selected."
    source_refs:
      - {
          capability: desktop-onboarding,
          requirement: canonical-instrument-identity,
          scenario: distinguish-index-from-equity,
        }
      - {
          capability: desktop-onboarding,
          requirement: safe-default-instrument,
          scenario: open-without-a-selection,
        }
    owning_stage: 1
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_v11_onboarding.py
        selector: tests/acceptance/test_v11_onboarding.py::test_index_identity_is_distinct_and_default_symbol_is_sse_composite
        assertion: The accepted desktop contract proves that the index cannot alias an equity and that a missing selection resolves to the canonical Shanghai Composite identity.
  - id: V11-R-002
    category: platform
    kind: user_visible
    behavior_key: four_step_first_run_happy_path
    acceptance: "First launch presents a four-step guided setup that prepares a source, selects a security, verifies daily data, and opens a usable market workspace with the Shanghai Composite immediately after completion."
    source_refs:
      - {
          capability: desktop-onboarding,
          requirement: guided-first-run-setup,
          scenario: complete-minimum-data-configuration,
        }
      - {
          capability: desktop-onboarding,
          requirement: guided-first-run-setup,
          scenario: continue-with-supported-deferred-choice,
        }
      - {
          capability: desktop-onboarding,
          requirement: ready-after-onboarding,
          scenario: open-default-market-workspace,
        }
    owning_stage: 1
    status: mapped
    evidence:
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/onboarding.spec.ts
        selector: first run wizard configures data and opens a usable default market
        assertion: The desktop browser flow completes the minimum setup without code entry and immediately reaches an interactive market chart for the Shanghai Composite.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/api/test_onboarding_api.py
        selector: tests/unit/api/test_onboarding_api.py::test_onboarding_api_four_step_contract_pins_then_syncs_and_completes
        assertion: The versioned desktop API completes preparation, selection, verified synchronization, completion, and initializes the default market workspace.
  - id: V11-R-003
    category: market
    kind: architecture
    behavior_key: whole_provider_onboarding_fallback
    acceptance: "Automatic setup attempts AKShare and BaoStock in order as whole-provider transactions, never mixes one provider catalog with another provider bars, pins the first fully verified provider, and exposes only stable recovery actions after failure."
    source_refs:
      - {
          capability: desktop-onboarding,
          requirement: whole-provider-fallback,
          scenario: fall-back-after-complete-provider-failure,
        }
      - {
          capability: desktop-onboarding,
          requirement: whole-provider-fallback,
          scenario: pin-first-fully-verified-provider,
        }
    owning_stage: 1
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/onboarding/test_service.py
        selector: tests/unit/onboarding/test_service.py::test_preparation_falls_back_only_after_the_whole_provider_attempt_fails
        assertion: The first free provider fails as one attempt before the second provider supplies the complete catalog transaction and becomes the sole pinned source.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/onboarding/test_service.py
        selector: tests/unit/onboarding/test_service.py::test_real_catalog_and_single_provider_daily_pin_are_required_for_completion
        assertion: Completion binds the catalog, selected instrument, synchronized daily bars, and verified evidence to one provider identity.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/onboarding/test_service.py
        selector: tests/unit/onboarding/test_service.py::test_provider_failures_expose_only_stable_recovery_codes
        assertion: Exhausted provider attempts preserve the setup step and expose only bounded retry, switch-provider, advanced, and demo recovery actions.
  - id: V11-R-004
    category: platform
    kind: user_visible
    behavior_key: demo_isolation_and_real_setup_recovery
    acceptance: "Read-only demo mode never completes onboarding or writes a real-data completion state; it survives restart and can be exited explicitly to resume a usable real-data setup."
    source_refs:
      - {
          capability: desktop-onboarding,
          requirement: read-only-demo-isolation,
          scenario: do-not-complete-real-setup,
        }
      - {
          capability: desktop-onboarding,
          requirement: read-only-demo-isolation,
          scenario: exit-demo-and-resume-real-setup,
        }
    owning_stage: 1
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/onboarding/test_service.py
        selector: tests/unit/onboarding/test_service.py::test_demo_mode_never_marks_onboarding_completed
        assertion: Demo mode remains in progress and completion is rejected until real synchronized evidence exists.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/onboarding/test_service.py
        selector: tests/unit/onboarding/test_service.py::test_demo_mode_survives_restart_and_exit_can_prepare_real_data
        assertion: A persisted demo session survives service restart and explicit exit returns to provider preparation without carrying a demo source.
      - state: existing
        runner: vitest
        kind: integration
        path: web/src/features/onboarding/OnboardingGate.test.tsx
        selector: restores persisted demo mode and can exit into a usable real-data setup
        assertion: The user interface labels restored demo state and provides an explicit path back to usable real-data setup.
  - id: V11-R-005
    category: platform
    kind: architecture
    behavior_key: atomic_onboarding_resume
    acceptance: "Each committed onboarding step is persisted atomically so a restart continues from the last committed step without replaying welcome, while an interrupted write preserves the prior valid revision."
    source_refs:
      - {
          capability: desktop-onboarding,
          requirement: persistent-progress,
          scenario: restart-from-last-committed-step,
        }
      - {
          capability: desktop-onboarding,
          requirement: persistent-progress,
          scenario: preserve-valid-state-after-interrupted-write,
        }
    owning_stage: 1
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/onboarding/test_store.py
        selector: tests/unit/onboarding/test_store.py::test_state_store_atomically_resumes_the_last_committed_revision
        assertion: An interrupted atomic replace leaves the previous valid onboarding revision loadable after restart.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/onboarding/OnboardingGate.test.tsx
        selector: resumes from a persisted step without replaying welcome
        assertion: The first-run interface resumes directly at the persisted selection step and does not replay the welcome screen.
  - id: V11-R-006
    category: platform
    kind: user_visible
    behavior_key: workspace_restore_and_safe_fallback
    acceptance: "The versioned workspace restores valid navigation and chart preferences after restart, while missing, expired, corrupt, illegal, or unavailable state falls back to the safe Shanghai Composite market workspace with a non-blocking notice."
    source_refs:
      - {
          capability: desktop-workspace,
          requirement: versioned-workspace-restore,
          scenario: restore-valid-session-after-restart,
        }
      - {
          capability: desktop-workspace,
          requirement: versioned-workspace-restore,
          scenario: fall-back-from-invalid-state,
        }
    owning_stage: 1
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/workspace/test_workspace_service.py
        selector: tests/unit/workspace/test_workspace_service.py::test_corrupt_and_illegal_route_states_return_specific_safe_notices
        assertion: Corrupt persisted JSON and illegal persisted routes return bounded recovery notices instead of restoring unsafe state.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/unit/workspace/test_workspace_service.py
        selector: tests/unit/workspace/test_workspace_service.py::test_expired_or_catalog_mismatched_workspace_safely_falls_back
        assertion: Expired or unavailable persisted instruments resolve to the canonical safe workspace without retaining invalid state.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/workspace-recovery.spec.ts
        selector: desktop restart restores the versioned workspace and persists valid changes
        assertion: A browser-level desktop restart restores the allowlisted route, instrument, period, adjustment, zoom, main chart, and subchart preferences.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/workspace-recovery.spec.ts
        selector: corrupt or illegal restored state safely falls back without navigating to it
        assertion: Browser-level corrupt and illegal workspace responses never navigate to unsafe state and visibly recover to the default market.
  - id: V11-R-007
    category: formula
    kind: user_visible
    behavior_key: formula_studio_desktop_accessibility
    acceptance: "Formula Studio remains operable inside the Windows desktop shell across System, Light, and Dark themes, supported narrow layouts, 200% effective scaling, keyboard focus, and its first-visit contextual guidance."
    source_refs:
      - {
          capability: desktop-user-experience,
          requirement: responsive-desktop-layout,
          scenario: high-display-scaling,
        }
      - {
          capability: desktop-user-experience,
          requirement: system-theme-and-non-color-expression,
          scenario: follow-system-theme-changes,
        }
      - {
          capability: desktop-user-experience,
          requirement: keyboard-and-focus-usability,
          scenario: complete-primary-journey-with-keyboard,
        }
      - {
          capability: desktop-onboarding,
          requirement: core-flow-contextual-guidance,
          scenario: first-visit-core-page,
        }
    owning_stage: 2
    status: mapped
    evidence:
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/theme-formula.spec.ts
        selector: Formula Studio keeps theme, focus and layout usable through 200% scaling
        assertion: The Formula Studio browser contract switches the actual Monaco theme, persists explicit preferences, keeps visible focus, and remains horizontally bounded at the 200 percent effective viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/contextual-guidance.spec.ts
        selector: Formula Studio guidance anchors have real geometry at 200 percent effective width
        assertion: Every Formula Studio tour step targets a non-zero drawable control while the dialog retains a trapped keyboard focus sequence and safe Escape dismissal.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/acceptance/test_tauri_desktop_shell.py
        selector: tests/acceptance/test_tauri_desktop_shell.py::test_tauri_shell_is_windows_only_single_window_and_uses_bundled_assets
        assertion: The packaged Windows desktop contract exposes one Tauri window that loads the same React asset tree containing Formula Studio without an external browser surface.
  - id: V11-R-008
    category: formula
    kind: security
    behavior_key: authenticated_desktop_formula_boundary
    acceptance: "Every Formula Studio API operation uses the authenticated Tauri host proxy without exposing session authority to React, while the desktop transport preserves the complete bounded v1 formula response and the sidecar retains parsing, future-data rejection, isolated execution, immutable versions, and provenance."
    source_refs:
      - {
          capability: windows-desktop-shell,
          requirement: sidecar-lifecycle-and-session-security,
          scenario: unauthorized-local-request,
        }
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: use-existing-core-capability-after-desktopization,
        }
    owning_stage: 2
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_sidecar_session.py
        selector: tests/integration/test_sidecar_session.py::test_formula_studio_requires_desktop_authority_and_preserves_provenance
        assertion: Formula template, validation, creation, and preview endpoints reject unauthenticated desktop traffic while an exact origin and bearer session preserves immutable formula and market provenance.
      - state: existing
        runner: vitest
        kind: integration
        path: web/src/app/tauriAdapter.test.ts
        selector: routes every Formula Studio operation through the host without exposing session authority
        assertion: Formula Studio operations use only the closed desktop_api_request command, never browser fetch, and expose neither bearer authority nor sidecar network coordinates to React.
      - state: existing
        runner: pytest
        kind: security
        path: tests/acceptance/test_formula_safety_boundary.py
        selector: tests/acceptance/test_formula_safety_boundary.py::test_future_or_repainting_formula_cannot_be_saved_or_backtested
        assertion: Future-looking and repainting drafts remain blocked from save, preview, and backtest without mutating the last valid immutable version.
  - id: V11-R-009
    category: formula
    kind: architecture
    behavior_key: desktop_formula_semantic_identity
    acceptance: "Desktop Formula Studio preserves v1 formula semantics: built-in MACD and a user-defined trading formula can be saved and previewed as subcharts with BUY and SELL signals, and backtesting consumes the exact same frozen formula version, checksum, engine, compatibility, dataset, route, manifest, and SignalSeries identity."
    source_refs:
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: use-existing-core-capability-after-desktopization,
        }
    owning_stage: 2
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_backtest_semantics.py
        selector: tests/acceptance/test_backtest_semantics.py::test_custom_saved_formula_persists_the_exact_computed_signal_series
        assertion: Built-in MACD and a custom trading formula each produce the exact SignalSeries and frozen provenance identity later consumed by the backtest engine.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/formula-studio.spec.ts
        selector: desktop MACD flow is atomic, versioned, parameterized, and safe
        assertion: Formula Studio saves and previews the built-in MACD as a subchart with numeric outputs and explicit BUY and SELL signals while unsupported or future-looking source remains blocked.
  - id: V11-R-010
    category: platform
    kind: user_visible
    behavior_key: bounded_actionable_desktop_recovery
    acceptance: "Loading, empty, offline, permission, error, and sidecar-unavailable states explain the user-visible cause and provide an actionable next step without exposing HTTP status, tracebacks, internal paths, or secrets; startup and runtime sidecar failures remain recoverable, and the restart limit still permits diagnostics and confirmed safe exit."
    source_refs:
      - {
          capability: desktop-user-experience,
          requirement: actionable-empty-error-and-offline-states,
          scenario: sidecar-unavailable,
        }
      - {
          capability: windows-desktop-shell,
          requirement: sidecar-lifecycle-and-session-security,
          scenario: unexpected-sidecar-exit,
        }
    owning_stage: 3
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_v11_desktop_recovery.py
        selector: tests/acceptance/test_v11_desktop_recovery.py::test_packaged_desktop_recovers_from_startup_and_runtime_sidecar_failures
        assertion: The accepted Rust host contract routes permission, spawn, health, version, and bounded-restart failures into stable recovery states instead of terminating or blanking the desktop window.
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_v11_desktop_recovery.py
        selector: tests/acceptance/test_v11_desktop_recovery.py::test_starting_or_recovery_exit_cleans_resources_without_a_ready_session
        assertion: Recovery exit is explicitly confirmed, releases failed managed resources, and exits without depending on a healthy sidecar session.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/shared/ActionableState.test.tsx
        selector: fails closed instead of rendering unsafe technical details
        assertion: The shared user state surface replaces unsafe HTTP, traceback, credential, and path material while withholding malformed failure identifiers.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/desktop/DesktopStartup.test.tsx
        selector: keeps the bounded terminal recovery state without offering another restart
        assertion: After the host reaches its restart limit, the desktop renders the stable terminal recovery copy without offering an unbounded retry.
  - id: V11-R-011
    category: security
    kind: security
    behavior_key: explicit_local_redacted_diagnostics_and_zero_telemetry
    acceptance: "A desktop-only authenticated action exports a strict, bounded, inspectable diagnostic package only to a location explicitly chosen by the user, states that the package remains local and was not uploaded, excludes credentials, prompts, usernames, paths, arbitrary files, and free-form logs with fail-closed final-byte validation, and the application contains no telemetry or automatic crash-upload fallback."
    source_refs:
      - {
          capability: desktop-user-experience,
          requirement: local-redacted-diagnostic-bundle,
          scenario: explicitly-export-diagnostic-bundle,
        }
      - {
          capability: desktop-user-experience,
          requirement: default-zero-telemetry,
          scenario: normal-application-use,
        }
    owning_stage: 3
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_diagnostic_export.py
        selector: tests/security/test_diagnostic_export.py::test_allowlisted_recursive_redaction_removes_all_private_classes
        assertion: Recursive diagnostic sanitizing irreversibly removes credentials, authorization, private prompts, usernames, hostnames, and private paths from allowlisted structures.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_diagnostic_export.py
        selector: tests/security/test_diagnostic_export.py::test_snapshot_is_fixed_schema_bounded_and_contains_no_free_form_log_message
        assertion: The desktop snapshot contains only fixed version, platform, health, configuration, structured-event, failure-ID, and local-privacy fields with no free-form log message.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_current_locked_application_has_no_telemetry_or_crash_sdk
        assertion: Locked dependencies and production source contain no telemetry or crash-reporting SDK or collection endpoint.
      - state: existing
        runner: pytest
        kind: security
        path: tests/unit/api/test_diagnostics_api.py
        selector: tests/unit/api/test_diagnostics_api.py::test_snapshot_is_post_only_and_requires_exact_desktop_authority
        assertion: Diagnostic generation is POST-only, desktop-session authenticated, and returns no session authority.
      - state: existing
        runner: vitest
        kind: integration
        path: web/src/app/diagnosticsExport.test.ts
        selector: requests a snapshot only after invocation and saves host-validated bytes locally
        assertion: An explicit user action obtains the strict authenticated snapshot, requires identical host validation, opens a local JSON save picker, and writes only the canonical validated bytes.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/app/App.test.tsx
        selector: offers a keyboard-accessible local diagnostic export with visible status
        assertion: The About surface exposes a keyboard-reachable diagnostic action and confirms that the saved package remained local and was not uploaded.
  - id: V11-R-012
    category: platform
    kind: user_visible
    behavior_key: exact_sha_windows_icon_and_packaged_desktop_matrix
    acceptance: "The rounded-window and three-candlestick identity remains readable from 16 through 256 pixels on light and dark Windows backgrounds and is shared by the installer and shell entries; an exact-SHA packaged Tauri candidate is installed, launched, keyboard-checked, theme-checked, and captured across the supported effective viewport matrix without claiming simulated viewport evidence as real OS DPI."
    source_refs:
      - {
          capability: desktop-user-experience,
          requirement: unified-desktop-icon,
          scenario: consistent-icons-across-desktop-entry-points,
        }
      - {
          capability: desktop-user-experience,
          requirement: responsive-desktop-layout,
          scenario: high-display-scaling,
        }
    owning_stage: 3
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/unit/test_icon_visual_evidence.py
        selector: tests/unit/test_icon_visual_evidence.py::test_icon_pixel_inventory_matches_reviewed_multisize_baseline
        assertion: The reviewed SVG, PNG, and ICO pixel identities produce deterministic light and dark contact-sheet evidence for every required Windows size.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_windows_desktop_evidence_contract.py
        selector: tests/unit/test_windows_desktop_evidence_contract.py::test_windows_candidate_runs_packaged_tauri_and_binds_visual_evidence
        assertion: Candidate A installs and launches the actual packaged Tauri app and binds native, theme, keyboard, effective-scale, and shell-icon evidence to the exact source SHA and tree.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_windows_desktop_evidence_contract.py
        selector: tests/unit/test_windows_desktop_evidence_contract.py::test_packaged_webview_matrix_is_explicitly_equivalent_not_real_os_dpi
        assertion: The automated matrix covers 100 through 200 percent effective viewports while explicitly separating CDP evidence from the remaining real Windows 10 and 11 DPI confirmation.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/acceptance/test_tauri_desktop_shell.py
        selector: tests/acceptance/test_tauri_desktop_shell.py::test_tauri_shell_is_windows_only_single_window_and_uses_bundled_assets
        assertion: The native host declares 640 by 360 as the supported minimum logical window; browser and installed evidence separately determine operability, and this configuration check is not Windows OS DPI proof.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 行情 light remains usable through the 100-200 percent effective viewport matrix
        assertion: Market renders the deterministic K-line canvas and OHLCV state, keeps the explicit no-formula default subchart reachable, and stays keyboard-reachable and non-overlapping in Light mode across every CSS-equivalent viewport without claiming Windows OS DPI.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 行情 dark remains usable through the 100-200 percent effective viewport matrix
        assertion: Market renders the deterministic K-line canvas and OHLCV state, keeps the explicit no-formula default subchart reachable, and stays keyboard-reachable and non-overlapping in Dark mode across every CSS-equivalent viewport without claiming Windows OS DPI.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 行情 System follows both color schemes without restart through the 100-200 percent effective viewport matrix
        assertion: Market follows both emulated System color schemes without restart at every CSS-equivalent viewport while retaining its rendered K-line, OHLCV, responsive, and accessibility contracts; this is not Windows theme or OS DPI proof.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 自定义公式 light remains usable through the 100-200 percent effective viewport matrix
        assertion: Formula Studio renders its actual K-line preview, formula subchart, and buy and sell signals while remaining keyboard-reachable and non-overlapping in Light mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 自定义公式 dark remains usable through the 100-200 percent effective viewport matrix
        assertion: Formula Studio renders its actual K-line preview, formula subchart, and buy and sell signals while remaining keyboard-reachable and non-overlapping in Dark mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 自定义公式 System follows both color schemes without restart through the 100-200 percent effective viewport matrix
        assertion: Formula Studio follows both emulated System color schemes without restart at every CSS-equivalent viewport while retaining its actual K-line, formula subchart, signal, responsive, and accessibility contracts.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 策略回测 light remains usable through the 100-200 percent effective viewport matrix
        assertion: Backtests complete the deterministic run, expose the report and populated trade-detail table, and remain keyboard-reachable and non-overlapping in Light mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 策略回测 dark remains usable through the 100-200 percent effective viewport matrix
        assertion: Backtests complete the deterministic run, expose the report and populated trade-detail table, and remain keyboard-reachable and non-overlapping in Dark mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 策略回测 System follows both color schemes without restart through the 100-200 percent effective viewport matrix
        assertion: Backtests follow both emulated System color schemes without restart at every CSS-equivalent viewport while retaining the completed report, trade-detail, responsive, and accessibility contracts.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 智能分析 light remains usable through the 100-200 percent effective viewport matrix
        assertion: Analysis completes the deterministic report, opens and closes both process and evidence drawers with focus restoration, and remains non-overlapping in Light mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 智能分析 dark remains usable through the 100-200 percent effective viewport matrix
        assertion: Analysis completes the deterministic report, opens and closes both process and evidence drawers with focus restoration, and remains non-overlapping in Dark mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 智能分析 System follows both color schemes without restart through the 100-200 percent effective viewport matrix
        assertion: Analysis follows both emulated System color schemes without restart at every CSS-equivalent viewport while retaining the completed report, both drawers, responsive, and accessibility contracts.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 任务中心 light remains usable through the 100-200 percent effective viewport matrix
        assertion: Task Center renders its dense metrics, filters, recent-task list, selected detail, status, and timeline without overlap in Light mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 任务中心 dark remains usable through the 100-200 percent effective viewport matrix
        assertion: Task Center renders its dense metrics, filters, recent-task list, selected detail, status, and timeline without overlap in Dark mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 任务中心 System follows both color schemes without restart through the 100-200 percent effective viewport matrix
        assertion: Task Center follows both emulated System color schemes without restart at every CSS-equivalent viewport while retaining its dense detail, responsive, and accessibility contracts.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 设置 light remains usable through the 100-200 percent effective viewport matrix
        assertion: Settings renders all source cards, daily priority controls, and the save bar without overlap in Light mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 设置 dark remains usable through the 100-200 percent effective viewport matrix
        assertion: Settings renders all source cards, daily priority controls, and the save bar without overlap in Dark mode at every CSS-equivalent viewport.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-theme-scale-matrix.spec.ts
        selector: 设置 System follows both color schemes without restart through the 100-200 percent effective viewport matrix
        assertion: Settings follows both emulated System color schemes without restart at every CSS-equivalent viewport while retaining its dense source, priority, save, responsive, and accessibility contracts.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-dialog-matrix.spec.ts
        selector: web workspace dialogs remain bounded, modal, keyboard-safe, and restore focus
        assertion: Guidance, About, model settings and disable confirmation, search popup, and market-pool dialogs use the native top layer where applicable; across Light, Dark, and both System resolutions at the 100 through 200 percent CSS-equivalent viewports plus the 640 by 360 logical minimum, they remain bounded, preserve natural DOM and visual Tab order without test-injected focus, keep dirty state behind safe confirmations, and restore initiating focus. This is not Windows OS DPI proof.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/desktop-dialog-matrix.spec.ts
        selector: desktop recovery, update, and exit dialogs use the same native safety contract
        assertion: Recovery and model-cost confirmation, update normal and pending states, and exit confirm, blocked, checkpointing, and timeout states keep safe component-selected focus, bounded actions, natural bidirectional Tab containment, fail-closed Escape behavior, and initiating-focus restoration across Light, Dark, and both System resolutions at the complete CSS-equivalent viewport matrix plus the logical minimum. This is browser top-layer evidence, not Windows OS DPI proof.
  - id: V11-R-013
    category: platform
    kind: user_visible
    behavior_key: checkpointed_desktop_exit_and_explicit_recovery
    acceptance: "Confirmed desktop exit stops new claims, discloses active work, and accepts shutdown only after queued-only durable state or an acknowledged safe checkpoint; timeout keeps the app open and claims resumable, while next startup focuses cancellation as the safe default without acting until an explicit choice and requires separate model-cost confirmation before resuming analysis."
    source_refs:
      - {
          capability: windows-desktop-shell,
          requirement: safe-exit-confirmation,
          scenario: confirm-exit,
        }
      - {
          capability: windows-desktop-shell,
          requirement: task-checkpoint-and-recovery-choice,
          scenario: active-tasks-on-confirmed-exit,
        }
      - {
          capability: windows-desktop-shell,
          requirement: task-checkpoint-and-recovery-choice,
          scenario: checkpoint-exceeds-ten-seconds,
        }
      - {
          capability: windows-desktop-shell,
          requirement: task-checkpoint-and-recovery-choice,
          scenario: next-start-handles-incomplete-tasks,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_sidecar_session.py
        selector: tests/integration/test_sidecar_session.py::test_desktop_shutdown_timeout_is_actionable_and_resumes_claims
        assertion: A missed checkpoint deadline returns an actionable retry response, cancels the pending checkpoint request, and reopens task claiming without terminating the sidecar.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_sidecar_session.py
        selector: tests/integration/test_sidecar_session.py::test_desktop_shutdown_accepts_only_after_durable_checkpoint_ack
        assertion: Active work can reach a durable desktop checkpoint before shutdown is accepted, with a persisted checkpoint event and no new claims.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_sidecar_session.py
        selector: tests/integration/test_sidecar_session.py::test_desktop_startup_recovery_requires_explicit_resume_or_cancel
        assertion: Startup reports categorized incomplete work and does not resume it before an explicit user choice.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_sidecar_session.py
        selector: tests/integration/test_sidecar_session.py::test_analysis_recovery_requires_explicit_cost_confirmation_and_is_idempotent
        assertion: Analysis recovery is idempotent and remains blocked until the user separately confirms possible model cost.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/desktop/DesktopExitGuard.test.tsx
        selector: shows blocked counts and requires a second explicit checkpoint confirmation
        assertion: The desktop confirmation discloses active work and requires a second explicit confirmation before requesting a checkpointed shutdown.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/desktop/DesktopTaskRecovery.test.tsx
        selector: defaults to cancel and offers explicit resume for incomplete work
        assertion: The incomplete-work dialog focuses the safe cancel action by default and keeps resume as an explicit alternative.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/desktop/DesktopTaskRecovery.test.tsx
        selector: requires an extra model-cost confirmation before resuming analysis
        assertion: Resuming analysis requires a separate user acknowledgement that model calls can incur cost.
  - id: V11-R-014
    category: platform
    kind: security
    behavior_key: authenticated_desktop_core_vertical_slice
    acceptance: "Backtest, Analysis, and Task Center operations join Formula Studio behind the authenticated Tauri host proxy: missing or wrong Origin or Bearer authority is rejected before dependencies run, authenticated requests preserve frozen workflow provenance, and React never receives session authority or sidecar coordinates."
    source_refs:
      - {
          capability: windows-desktop-shell,
          requirement: sidecar-lifecycle-and-session-security,
          scenario: unauthorized-local-request,
        }
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: use-existing-core-capability-after-desktopization,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: security
        path: tests/integration/test_desktop_core_workflows.py
        selector: tests/integration/test_desktop_core_workflows.py::test_desktop_authority_protects_each_core_workflow_before_dependencies_run
        assertion: Backtest, analysis, and task endpoints reject missing or incorrect desktop authority before invoking their workflow dependencies.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_desktop_core_workflows.py
        selector: tests/integration/test_desktop_core_workflows.py::test_authenticated_desktop_runs_backtest_analysis_and_safe_task_center
        assertion: An authenticated desktop session runs a real formula-to-backtest-to-analysis-to-task-center slice with frozen and redacted provenance.
      - state: existing
        runner: vitest
        kind: integration
        path: web/src/app/tauriAdapter.test.ts
        selector: routes Backtest, Analysis, and Task Center operations only through host IPC
        assertion: React routes every non-formula core request through the closed Tauri command without receiving sidecar authority or network coordinates.
  - id: V11-R-015
    category: publication
    kind: publication
    behavior_key: unsigned_prerelease_exact_proof_reuse
    acceptance: "Unsigned v1.1 alpha and beta publication consumes the exact tagged main proof and exact-version Windows candidate, verifies their identities and digests, and publishes only clearly unsigned Windows prerelease assets without rerunning source tests or rebuilding; stable and rc tags remain fail-closed until the separate trusted signing and installed-matrix chain exists."
    source_refs:
      - {
          capability: trusted-windows-distribution,
          requirement: independent-candidate-build-and-no-duplicate-release,
          scenario: release-consumes-main-proof,
        }
      - {
          capability: trusted-windows-distribution,
          requirement: trusted-code-signing-and-smartscreen-gate,
          scenario: publish-unsigned-test-package,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_v11_release_workflow.py
        selector: tests/unit/test_v11_release_workflow.py::test_v11_release_has_only_exact_proof_reuse_and_unsigned_publish_jobs
        assertion: The current v1.1 release workflow contains only tag policy, exact proof and candidate verification, and unsigned prerelease publication jobs.
      - state: existing
        runner: pytest
        kind: absence
        path: tests/unit/test_v11_release_workflow.py
        selector: tests/unit/test_v11_release_workflow.py::test_v11_release_cannot_rebuild_retest_or_publish_legacy_platforms
        assertion: The release workflow contains no source test, binary rebuild, Inno Setup, macOS, Linux, Android, or legacy stable publication path.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_v11_release_workflow.py
        selector: tests/unit/test_v11_release_workflow.py::test_v11_tag_policy_fails_closed_until_trusted_stable_chain_exists
        assertion: Stable and rc tags are visibly rejected while only version-matched unsigned alpha and beta tags are accepted.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_v11_release_workflow.py
        selector: tests/unit/test_v11_release_workflow.py::test_v11_prerelease_asset_selection_is_version_agnostic_and_windows_only
        assertion: Unsigned prerelease publication binds the tag version to the exact Windows candidate name and rejects every unsupported asset family.
  - id: V11-R-016
    category: backtest
    kind: user_visible
    behavior_key: desktop_backtest_transport_equivalence_matrix
    acceptance: "Authenticated desktop transport matches the current direct service across twelve normal-market cells combining MACD and custom formulas, single-symbol and pool scopes, and daily, weekly, and 60-minute periods: canonical reports, trades, open positions, snapshots, result hashes, and signal-series identities match, while a persisted pool checkpoint resumed by a fresh worker matches its uninterrupted current-service baseline. This transport regression proof is not an independent v1.0 oracle and does not claim complete suspension or price-limit edge coverage."
    source_refs:
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: use-existing-core-capability-after-desktopization,
        }
      - {
          capability: windows-desktop-shell,
          requirement: task-checkpoint-and-recovery-choice,
          scenario: active-tasks-on-confirmed-exit,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_desktop_backtest_transport_equivalence.py
        selector: tests/acceptance/test_desktop_backtest_transport_equivalence.py::test_authenticated_desktop_transport_matches_current_service_for_12_normal_market_cells
        assertion: All twelve normal-market formula, scope, and period cells run through the current direct service and authenticated desktop transport and produce canonical-equivalent reports, trades, open positions, snapshots, result hashes, and signal series; they are not an independent v1.0 or abnormal-market oracle.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/acceptance/test_desktop_backtest_transport_equivalence.py
        selector: tests/acceptance/test_desktop_backtest_transport_equivalence.py::test_pool_checkpoint_resumes_on_new_worker_with_uninterrupted_result
        assertion: A persisted pool checkpoint is resumed through a fresh task repository and worker and finishes with the same canonical report, snapshot, result hash, and signal series as its uninterrupted current-service baseline.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/backtest.spec.ts
        selector: desktop market prefill runs MACD through report, replay, and export
        assertion: The desktop Backtest workspace accepts the Market handoff and completes the visible MACD report, replay, and export journey.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/backtest.spec.ts
        selector: custom formula discloses and completes a partial pool
        assertion: The desktop Backtest workspace visibly submits a versioned custom formula pool and renders its partial terminal report without hiding failures.
  - id: V11-R-017
    category: analysis
    kind: security
    behavior_key: desktop_analysis_task_research_safety
    acceptance: "Analysis and Task Center share the exact authenticated desktop authority while preserving fixed role prompts, untrusted external-data isolation, masked domestic-model credentials, source and cutoff provenance, partial and insufficient degradation, safe task projections, non-investment-advice boundaries, module isolation from formulas and backtests, contextual guidance, and a second explicit model-cost confirmation before analysis recovery."
    source_refs:
      - {
          capability: first-run-onboarding,
          requirement: core-flow-contextual-guidance,
          scenario: first-visit-core-page,
        }
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: use-existing-core-capability-after-desktopization,
        }
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: user-seeks-live-trading-or-investment-advice,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_v11_analysis_task_journey.py
        selector: tests/acceptance/test_v11_analysis_task_journey.py::test_desktop_analysis_and_task_center_share_only_auditable_safe_state
        assertion: A complete five-role DeepSeek-compatible workflow keeps malicious external text in untrusted data blocks, uses the current fixed role prompts, records non-empty prompt and request hashes with evidence provenance, emits the disclaimer, and exposes only safe task and event projections.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/integration/test_sidecar_session.py
        selector: tests/integration/test_sidecar_session.py::test_analysis_and_task_center_share_exact_desktop_session_authority
        assertion: Model settings, analysis, and safe Task Center endpoints reject missing or wrong desktop authority and share the exact Origin and Bearer session without leaking private task payloads.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/analysis.spec.ts
        selector: configures a model and completes traceable analysis, retry, and insufficient flows
        assertion: One visible desktop journey covers masked model setup, traceable complete evidence, partial retry, insufficient recovery, Analysis and Task guidance anchors, safe Task Center projection, and formula or backtest isolation.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/desktop/DesktopTaskRecovery.test.tsx
        selector: requires an extra model-cost confirmation before resuming analysis
        assertion: Analysis recovery keeps cancellation focused, performs no request after continue or return, and posts confirm_analysis_cost only after the separate explicit cost acknowledgement.
  - id: V11-R-018
    category: security
    kind: security
    behavior_key: anonymous_update_and_local_diagnostics_privacy_guard
    acceptance: "The desktop privacy boundary is machine-verifiable and fail-closed for the active trusted-updater-foundation phase: diagnostics remain explicit, allowlisted, recursively redacted, local-only, and abort on unknown nested data; telemetry, automatic crash or diagnostic upload, runtime updater activation, and stable host or device identifiers are absent; enumerated network-capable imports and direct primitives are confined to exact reviewed paths, including aliased Python dynamic imports, the reviewed default-off Rust updater host, Rust network-client aliases, and Web sendBeacon, and all Tauri configuration and capability files are recursively checked for updater permissions and unsafe symlink ancestors; the fixed updater contract permits only anonymous requests without usage behavior or local-data digests, requires explicit user confirmation, and exact candidate and main proof evidence binds the privacy policy, verifier, schema, and updater source inputs."
    source_refs:
      - {
          capability: desktop-user-experience,
          requirement: local-redacted-diagnostic-bundle,
          scenario: redaction-scan-finds-sensitive-field,
        }
      - {
          capability: desktop-user-experience,
          requirement: default-zero-telemetry,
          scenario: application-crashes,
        }
      - {
          capability: desktop-user-experience,
          requirement: default-zero-telemetry,
          scenario: perform-update-check,
        }
      - {
          capability: trusted-windows-distribution,
          requirement: non-mandatory-trusted-in-app-update,
          scenario: update-check-privacy,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_diagnostic_export.py
        selector: tests/security/test_diagnostic_export.py::test_allowlisted_recursive_redaction_removes_all_private_classes
        assertion: Recursive diagnostics remove every credential, prompt, user, host, and path class before any local export can succeed.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_diagnostic_export.py
        selector: tests/security/test_diagnostic_export.py::test_unknown_or_unserializable_diagnostic_material_fails_closed
        assertion: Unknown or unserializable diagnostic material aborts export instead of bypassing the recursive allowlist and redaction boundary.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_current_locked_application_has_no_telemetry_or_crash_sdk
        assertion: The locked production tree, manifests, recursive Tauri configuration, network paths, and privacy policy satisfy the fail-closed privacy verifier.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_production_worker_ids_use_random_session_identity_not_host_identity
        assertion: All production worker entry points call the shared ephemeral identity helper and never derive identifiers from the hostname.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_new_worker_id_has_random_unique_session_shape
        assertion: The shared worker identity helper produces unique session-scoped identifiers with a fixed high-entropy hexadecimal shape.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_device_identity_automatic_upload_or_updater_enablement_fails_closed
        assertion: Static privacy verification rejects stable installation identity, automatic diagnostic upload, unreviewed updater dependencies, updater capabilities, and runtime updater configuration.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_privacy_policy_is_required_and_future_updates_must_remain_anonymous
        assertion: The canonical policy is required semantically and refuses any future updater contract that changes anonymous requests into a stable device identity.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_phase_policy_and_recursive_tauri_json_fail_closed
        assertion: An unreviewed policy phase or updater permission hidden in any nested Tauri capability file fails the privacy gate.
      - state: existing
        runner: pytest
        kind: security
        path: tests/unit/test_trusted_updater_release.py
        selector: tests/unit/test_trusted_updater_release.py::test_missing_fixed_production_public_key_fails_closed
        assertion: Stable updater release eligibility remains blocked until the fixed production Tauri public key exists and real artifact verification can run.
      - state: existing
        runner: vitest
        kind: component
        path: web/src/features/desktop/DesktopUpdateNotice.test.tsx
        selector: fails closed and hides expanded update protocol payloads
        assertion: The desktop update UI rejects expanded host payloads, does not expose device identity, and cannot turn unreviewed data into an update offer.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_capabilities_symlink_ancestor_fails_closed
        assertion: A symlinked Tauri capabilities root and configuration reached through that unsafe ancestor both fail the privacy gate.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_python_network_aliases_require_an_exact_allowlisted_path
        assertion: Python AST inspection recognizes enumerated aliased network-capable imports, including dynamic import aliases, and rejects them outside an exact reviewed path.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_direct_network_aliases_require_an_exact_allowlisted_path
        assertion: Enumerated Rust network-client aliases and the Web sendBeacon primitive are rejected outside exact reviewed paths.
      - state: existing
        runner: pytest
        kind: security
        path: tests/security/test_zero_telemetry.py
        selector: tests/security/test_zero_telemetry.py::test_snapshot_and_failure_collection_make_no_network_attempt
        assertion: Diagnostic snapshot collection and its failure paths make no network connection attempts.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_windows_desktop_evidence_contract.py
        selector: tests/unit/test_windows_desktop_evidence_contract.py::test_windows_candidate_runs_packaged_tauri_and_binds_visual_evidence
        assertion: Candidate A runs the privacy verifier before packaging evidence and binds the workflow, privacy verifier, and privacy policy hashes into its exact-SHA evidence manifest.
  - id: V11-R-019
    category: backtest
    kind: operational
    behavior_key: desktop_backtest_v1_oracle_equivalence
    acceptance: "The current backtest engine preserves v1.0.0 business semantics against an independent offline oracle bound to the immutable v1.0.0 commit and tree: twelve MACD or parameterized-custom formula, single or pool, daily or weekly or 60-minute cells plus A-share constraint chains, open-position costs, and partial-pool gaps match exactly after only a closed, canonical allowlist of versioned identity and price normalization; a fresh-worker desktop checkpoint resume matches the uninterrupted v1.0.0 oracle, and any source, generator, input, allowlist, case, or payload drift fails closed. The existing authenticated twelve-cell desktop transport matrix and real MACD and custom-formula Playwright journeys carry this proof through the host boundary into the visible desktop UI without treating current-to-current transport comparison as the oracle."
    source_refs:
      - {
          capability: windows-desktop-shell,
          requirement: core-capability-compatibility-baseline,
          scenario: use-existing-core-capability-after-desktopization,
        }
      - {
          capability: windows-desktop-shell,
          requirement: task-checkpoint-and-recovery-choice,
          scenario: active-tasks-on-confirmed-exit,
        }
    owning_stage: 4
    status: mapped
    evidence:
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_v1_backtest_oracle.py
        selector: tests/unit/test_v1_backtest_oracle.py::test_frozen_v1_oracle_is_self_verifying_and_covers_every_input
        assertion: The frozen oracle integrity-validates its pinned source identity, exact input matrix, generator, canonical allowlist, complete case inventory, and per-case semantic payload; Git review and main proof bind the files to the repository history.
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_v1_backtest_oracle_equivalence.py
        selector: tests/acceptance/test_v1_backtest_oracle_equivalence.py::test_current_backtest_business_semantics_match_frozen_v1_oracle
        assertion: All twelve normal-market cells and three abnormal or cost cases execute in the current engine and match the independent v1.0.0 business-semantic oracle.
      - state: existing
        runner: pytest
        kind: integration
        path: tests/acceptance/test_v1_backtest_oracle_equivalence.py
        selector: tests/acceptance/test_v1_backtest_oracle_equivalence.py::test_resumed_current_pool_run_matches_uninterrupted_v1_oracle
        assertion: A pool backtest paused at a durable desktop checkpoint and resumed by a fresh task repository and worker matches the uninterrupted v1.0.0 oracle apart from auditable recovery logs.
      - state: existing
        runner: pytest
        kind: acceptance
        path: tests/acceptance/test_desktop_backtest_transport_equivalence.py
        selector: tests/acceptance/test_desktop_backtest_transport_equivalence.py::test_authenticated_desktop_transport_matches_current_service_for_12_normal_market_cells
        assertion: The authenticated host transport carries all twelve current-service formula, scope, and period cells into the desktop boundary, completing the explicit v1 oracle to current service to desktop transport chain.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/backtest.spec.ts
        selector: desktop market prefill runs MACD through report, replay, and export
        assertion: A real visible desktop journey carries the oracle-covered MACD flow through Market handoff, report, replay, and export.
      - state: existing
        runner: playwright
        kind: acceptance
        path: web/e2e/backtest.spec.ts
        selector: custom formula discloses and completes a partial pool
        assertion: A real visible desktop journey carries the oracle-covered custom-formula pool flow through partial outcome disclosure and terminal reporting.
      - state: existing
        runner: pytest
        kind: contract
        path: tests/unit/test_windows_desktop_evidence_contract.py
        selector: tests/unit/test_windows_desktop_evidence_contract.py::test_packaged_backtest_matrix_uses_webview_host_ipc_and_new_worker_resume
        assertion: The Windows candidate contract requires the installed WebView host-IPC matrix, refuses demo and router bypasses, checkpoints through the desktop shutdown protocol, restarts the packaged sidecar, and verifies a different Worker identity after resume.
non_goals: []
